#!/bin/bash

git add .

git commit -m "Create a script that prints all possible combinations of two letters, except oo"

git update-index --chmod=+x 12-combinations

git add .

git commit -m "Create a script that prints all possible combinations of two letters, except oo"

git push
