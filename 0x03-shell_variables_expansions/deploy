#!/bin/bash

git add .

git commit -m "The path to success is to take massive, determined action"

git update-index --chmod=+x 2-path

git add .

git commit -m "The path to success is to take massive, determined action"

git push