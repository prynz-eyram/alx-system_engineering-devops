#!/bin/bash

git add .

git commit -m "Every addition to true knowledge is an addition to human power"

git update-index --chmod=+x 8-true_knowledge

git add .

git commit -m "Every addition to true knowledge is an addition to human power"

git push
